name: Weather App CI

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.samyam }}
          password: ${{ secrets.samyam }}

      - name: Build and push frontend
        run: |
          docker build -t ${{ secrets.samyam }}/weather-frontend ./frontend
          docker push ${{ secrets.samyam }}/weather-frontend

      - name: Build and push backend
        run: |
          docker build -t ${{ secrets.samyam }}/weather-backend ./backend
          docker push ${{ secrets.samyam }}/weather-backend
